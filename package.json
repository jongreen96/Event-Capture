{
  "name": "event-capture-mono",
  "private": true,
  "workspaces": [
    "client/*",
    "server/*"
  ],
  "scripts": {
    "start": "concurrently 'bun run start:client' 'bun run start:server'",
    "start:client": "cd client && bun start",
    "start:server": "cd server && bun dev"
  }
}
